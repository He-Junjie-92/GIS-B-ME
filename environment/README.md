# Python environment

`requirements.txt` contains the core reading and validation dependencies. `requirements-generator.txt` adds the optional GIS dependencies needed by the frozen generator workflow. Python 3.10 or newer is recommended.
