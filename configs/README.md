# Released generation configurations

These 135 JSON files are the frozen generation-input snapshots for GIS-B-ME v1.0. They are identical to the copies under `metadata/generation_configs/`. Use them with `code/generator/` and the frozen GIS assets in the full release.
