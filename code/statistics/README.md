# Statistics code

Run `python code/statistics/summarize_release.py` from any directory to recompute
compact instance, task, building and reference-result counts from the published
files. Use `--output summary.json` to save the result. The script does not read
algorithm populations, Pareto fronts or other excluded intermediate artifacts.
